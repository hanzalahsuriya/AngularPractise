import { DropdownMenu2Directive } from './dropdown-menu2.directive';

describe('DropdownMenu2Directive', () => {
  it('should create an instance', () => {
    const directive = new DropdownMenu2Directive();
    expect(directive).toBeTruthy();
  });
});
